import { styled } from '@mui/system'

export const TLogo = styled('img')(({ theme }) => ({
    padding: '1.2rem',
    backgroundColor: '#D9D9D9',
    marginBottom: '2rem',
    borderRadius: '50%',
}))